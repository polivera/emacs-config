;;; marginalia.el --- Marginalia annotations -*- lexical-binding: t; -*-

(use-package marginalia
  :ensure (:wait t)
  :demand t
  :init
  (marginalia-mode 1))

(provide 'marginalia)

;;; marginalia.el ends here
